# Public Supabase values only. These are safe for browser code when RLS is correct.
# Never commit service_role, secret keys, database passwords, or broker API keys.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
